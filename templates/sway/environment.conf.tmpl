# Generated by Rice {{ .Version }} for generation {{ .Generation }}.
# Theme: {{ .Theme.Name }}
#
# Read by the systemd user manager at login. Changes here need a fresh
# session, not a Sway reload.

XCURSOR_THEME={{ .Cursor.Theme }}
XCURSOR_SIZE={{ .Cursor.Size }}
{{- if .Config.Components.Qt }}
{{- with .Config.Qt.PlatformTheme }}
QT_QPA_PLATFORMTHEME={{ . }}
{{- end }}
{{- end }}
{{- range $key, $value := .Sway.Environment }}
{{ $key }}={{ $value }}
{{- end }}
